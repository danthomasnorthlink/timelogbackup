{
  "name": "timelog-ts",
  "version": "1.0.0",
  "description": "communicates with slackbot and handles database connections",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node ./build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.17",
    "@vercel/node": "^3.0.19",
    "dotenv": "^16.4.4",
    "nodemon": "^3.0.3",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.1",
    "querystring": "^0.2.1"
  }
}
